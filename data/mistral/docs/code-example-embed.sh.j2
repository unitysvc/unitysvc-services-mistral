curl -X POST "$SERVICE_BASE_URL" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $UNITYSVC_API_KEY" \
     -d '{"model": "${MODEL:-{{offering.name}}}", "input": ["Embed this sentence.", "As well as this one."]}'